/*
 * Public frontend configuration.
 *
 * Market-data credentials, if ever needed, belong only in Netlify's
 * environment variables, never here.
 */
window.POCKET_ISA_API_BASE = 'https://pocket-isa.netlify.app';